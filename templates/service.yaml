apiVersion: v1
kind: Service
metadata:
  name: {{ include "kotal.fullname" . }}
  labels:
    {{- include "kotal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kotal.selectorLabels" . | nindent 4 }}
